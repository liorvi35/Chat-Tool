#ifndef _CHAT_H_

    /**
     * @brief a flag to mark the header file (so wont be included more than once in total)
    */
    #define _CHAT_H_

    /**
     * @brief a port for client-server connection
    */
    #define PORT "62530"

    /**
     * @brief server's ip address for client's connection
    */
    #define SERVER_IP_ADDR "127.0.0.1"

    /**
     * @brief number of clients that server can handle
    */
    #define NUM_OF_CLIENTS 300


#endif